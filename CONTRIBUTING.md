# 参与进来

欢迎的：修 bug、改错别字、补浏览器兼容、补无障碍、订正工程数据（扭矩、规格、装配顺序）。

**订正工程数据尤其欢迎。** 这份清单里的每一个数都可能错，
而错了的后果是有人照着它把车装坏。发现哪一条不对，开个 Issue 说清出处就行。

先聊聊再动手的：加运行时依赖（目前只有 three 一个）、换框架、
加新的交互原语、大改课程编排。这几件都不是不行，但改动面太大，
直接提 PR 大概率白写。

## 先跑起来

```bash
npm install
```

```bash
npm run dev
```

看两份文档：[docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) 讲怎么改、
[docs/CONTRACT.md](docs/CONTRACT.md) 讲各层之间的约定 —— **改接口先改它**。

## 提交之前

```bash
npm run check:code
```

lint、单测、清单对账、构建，四条。CI 每个 PR 都跑这一条。

动了三维、步骤、界面版式的，本地再跑一遍冒烟：

```bash
npm run smoke
```

它会起产物、双画幅、按用户那条路把整台车装完，再逐条对账。
分钟级，所以只在推到 `main` 之后跑或手动触发，不卡 PR。

## 几条约定

**清单是唯一事实来源。** 装配方向、行程、扭矩、依赖顺序都写在
`assets/bike.manifest.json`，代码里不许再写第二份。改了清单跑 `npm run verify`。

**取景不写常量。** 每一步的机位与取景由 `steps/util.js` 的 `shot()` 从几何现算。
手写过一版常量，二十来个数里实测有六个是错的。复量走 `.analysis/p-fit.js`。

**世界方向不能直接加到 position 上。** 模型里每个件的父级基底都不是单位阵。
一律走 `ctx.slide.park()`。这一条踩过：前轮曾横着飞出 19 cm。

**颜色只在 `src/ui/styles/tokens.css` 里定义。** 组件只引用语义名。
曾经有过两套并行的界面 CSS，同名令牌取值不同、靠 import 顺序决定谁赢。

**文案能少则少。** 这一份要让人看懂，不是让人读懂。
常驻文字只有步名与一行旁白；说明卡只写「物理原因看不见」的那几处。

**不手改 `dist/`。** 那是产物。

## 提交与 PR

提交信息与 PR 标题用中文，一句话说清做了什么。不用前缀，不用 emoji。

PR 正文三段：

```
改了什么
为什么
怎么验的
```

第三段最要紧。「跑了 npm run check」是合格的；
动了三维或版式的，附一张截图（`node .analysis/shots.mjs <步骤id>` 能直接出图）。

## 关于授权

本项目分三层授权，其中第三层不归维护者管 —— 详见 [COMMERCIAL.md](COMMERCIAL.md)。

提交贡献即表示你同意：你保留自己的著作权，同时授予维护者在
AGPL-3.0（代码）与 CC BY-NC-SA 4.0（内容）框架下使用的权利，包含商业授权。
不同意请在 PR 里说明。
