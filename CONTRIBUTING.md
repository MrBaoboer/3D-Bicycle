# 参与进来

欢迎的：修 bug、改错别字、补浏览器兼容、补无障碍、订正工程数据（规格、螺距、装配顺序）。

**订正工程数据尤其欢迎。** 清单里的每一个数都可能错，
而错了的后果是有人照着它把车装坏。发现哪一条不对，开个 Issue 说清出处就行。

先开 Issue 聊过再动手的：加运行时依赖（目前只有 three 一个）、换框架、
加新的交互原语、大改课程编排。这几件改动面大，先对齐方向能省一轮返工。

## 先跑起来

```bash
npm install && npm run dev
```

动手之前看一遍 [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md)，它就是这个项目的约定。
PR 里最常撞上的是这四条：

- **清单是唯一事实来源** —— 装配方向、行程、螺距、依赖顺序都在
  `assets/bike.manifest.json`，代码里不写第二份（[清单](docs/DEVELOPMENT.md#清单)）
- **取景不写常量** —— 每一步的机位由 `steps/util.js` 从几何现算
- **世界方向不能直接加到 position 上** —— 一律走 `ctx.slide.park()`
- **颜色只在 `src/ui/styles/tokens.css` 里定义**，组件只引用语义名

文案上只有一条：**能少则少。** 常驻文字只有步名与一行旁白，
说明卡只写「物理原因看不见」的那几处。

## 提交之前

```bash
npm run check:code
```

lint、单测、清单对账、构建，四条。CI 每个 PR 都跑这一条。

动了三维、步骤、界面版式的，本地再跑一遍冒烟（分钟级，只在推到 `main` 之后由 CI 跑）：

```bash
npm run smoke
```

改了取景的，`npm run frames` 前后各跑一遍对着看。改了外观的，附一张截图
（`npm run build && npm run shots` 从产物里实拍六张）。不手改 `dist/`。

## 提交与 PR

提交信息与 PR 标题用中文，一句话说清做了什么。不用前缀，不用 emoji。

PR 正文三段：改了什么、为什么、怎么验的。第三段写上一节里你真正跑过的那几条。

## 关于授权

本项目分三层授权，其中整车模型那一层不归维护者管，详见 [COMMERCIAL.md](COMMERCIAL.md)。

提交贡献即表示你同意：你保留自己的著作权，同时授予维护者在
AGPL-3.0（代码）与 CC BY-NC-SA 4.0（内容）框架下使用的权利，包含商业授权。
不同意请在 PR 里说明。
